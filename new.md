# 네이버 블로그 홈페이지형 스킨(투명 위젯 홈형 블로그) 제작 실행 가이드 — "쑥쑥아리's 꿀잼 라이프로그"

## TL;DR
- 홈페이지형 스킨은 별도 기능이 아니라, **스킨배경(가로 최대 3000px)에 와이드 대문 이미지를 깔고 + 투명 PNG 위젯(가로 170px 고정·세로 최대 600px)으로 클릭 영역을 얹는** 조합 기술이다. 즉 "배경 이미지 = 보이는 디자인", "투명 위젯 = 보이지 않는 버튼"이라는 2층 구조다.
- 2026년 현재 경로는 **관리 > 꾸미기 설정 > 레이아웃·위젯 설정**(레이아웃·위젯 직접등록)과 **관리 > 꾸미기 설정 > 세부 디자인 설정 > 리모컨 > 스킨배경 > 직접등록**(대문 이미지 업로드)으로 동일하게 유지되고 있다.
- 가장 큰 한계는 **PC 전용**이라는 점이다. 모바일 앱/웹에서는 PC 위젯·스킨배경이 전혀 적용되지 않으므로, 모바일은 블로그 앱의 **[홈 편집]** 커버·외부채널 기능으로 별도 구성해야 한다. 수원→가산 이전에 따른 SEO 연속성은 스킨이 아니라 본문/지역 키워드·프로필·연락처 정보로 관리해야 한다.

## Key Findings
1. **개념**: 홈페이지형 스킨 = 1단 레이아웃 + 와이드 스킨배경 + 투명 위젯(이미지맵/링크). 일반 스킨은 타이틀 영역(가로 966px)에만 이미지를 넣는 반면, 홈형은 스킨배경 전체(좌우 여백 포함, 가로 최대 3000px)를 활용한다.
2. **위젯 핵심 수치**: 가로 **170px 고정**, 세로 **최대 600px**, 한 줄 **최대 5개**.
3. **타이틀 처리**: 세부 디자인 설정에서 '블로그 제목 표시'를 끄거나, 레이아웃에서 타이틀 영역을 비활성화/투명 처리해 배경 이미지가 메뉴처럼 보이게 한다.
4. **모바일 분리**: PC 홈형 스킨은 모바일 미적용 → 앱 [홈 편집]에서 커버 이미지·커버 스타일·외부채널 별도 설정.

## Details

### 1. 홈페이지형 스킨의 개념과 일반 스킨과의 차이
- **일반 스킨**: 네이버가 제공하는 스킨을 고르거나 타이틀 영역(가로 최대 966px, 세로 50~600px)에만 이미지를 넣는 방식. 화면 가운데 본문 영역만 꾸며진다.
- **홈페이지형 스킨**: 타이틀 영역에 갇히지 않고 **스킨배경 영역 전체**(가로 최대 3000px, 좌우 여백 포함)에 대문 이미지를 깔아 홈페이지 메인처럼 보이게 한다. 그 위에 **투명 위젯**을 얹어 "카페·맛집·여행·지원금" 같은 메뉴 버튼을 클릭 가능한 영역으로 만든다.
- **원리(2층 구조)**: ① 보이는 층 = 스킨배경 이미지(디자인·메뉴 그림). ② 보이지 않는 층 = 투명 PNG 위젯(클릭 영역). 사용자는 배경의 '맛집' 글자를 보고 그 위치를 클릭하면, 같은 위치에 놓인 투명 위젯의 링크가 실행된다. 메뉴까지 포함한 전체 화면이 사실상 하나의 큰 배너이며, 메뉴 위치에 '링크가 걸린 투명 이미지'를 정확히 겹쳐 놓는 것이 핵심이다.

### 2. 제작 전 준비물
- **이미지 제작 도구**: 포토샵 / 피그마(무료, 좌표 설계에 유리) / 캔바(템플릿·로고 요소·투명 배경 내보내기 지원) 중 택1. 프론트엔드 개발자라면 피그마가 좌표(x, y)를 px 단위로 정확히 읽을 수 있어 위젯 좌표 설계에 가장 적합하다.
- **배경 이미지 권장 사이즈**: 스킨배경은 "가로는 3000픽셀, 세로는 원하는 높이"로 제작하되(Everyday Vignettes '네이버 블로그 스킨 만들기' 가이드 확인), 실제로 항상 보이는 안전 영역은 가운데 약 850px 내외이다. 따라서 **전체 캔버스 3000px 중 가운데 850~966px 안에 핵심 디자인·메뉴를 배치**하고, 좌우는 배경/패턴으로 채운다. 와이드 타이틀용으로는 가로 1920px(세로 50~600px)도 흔히 쓰인다.
- **위젯 이미지**: Canva 공식 가이드('Canva로 홈페이지형 블로그 만들기')는 "사용자 지정 크기에 네이버 위젯의 최대 사이즈인 가로 170 * 세로 600 픽셀로 사이즈를 지정"하라고 안내한다. 캔바에서 170×600으로 만들고 투명 배경으로 내보낸다.
- **메뉴 버튼 좌표 설계**: 피그마에서 3000px(또는 1920px) 프레임을 만들고, 가운데 영역에 메뉴 5개(카페/맛집/여행/지원금/+소개 등)를 배치한 뒤 각 버튼의 좌상단 좌표와 폭·높이를 기록해 둔다. 이 좌표가 나중에 위젯 배치와 이미지맵 coords의 기준이 된다.

### 3. 2026년 현재 관리 화면 기준 단계

**A. 레이아웃·위젯 설정**
- 경로: **블로그 우측 상단 [내 메뉴] > 관리 > 꾸미기 설정 > 레이아웃·위젯 설정**.
- 레이아웃: 홈형에서는 **1단 레이아웃**을 선택하고 전체정렬은 중앙, 글 영역은 넓게로 설정. 1단은 본문 영역의 상단·하단에 위젯을 둘 수 있어 홈페이지 바로가기 배너를 구현하기 좋다(2·3단은 좌/우측으로 위젯 위치가 제한됨).
- 타이틀 영역: 메뉴 사용 설정에서 **'타이틀' 체크를 해제**하거나 타이틀 영역을 쓰지 않는 레이아웃을 선택한다.

**B. 스킨 배경 이미지 등록**
- 경로: **관리 > 꾸미기 설정 > 세부 디자인 설정 > (우측 리모컨) 스킨배경 > 직접등록 > 파일 등록** → 제작한 대문 이미지 업로드 → 하단 영역(패턴) 색을 배경과 맞춤 → 적용.
- 와이드(좌우 꽉 찬) 대문은 타이틀이 아니라 **반드시 '스킨배경'에 등록**해야 한다. 타이틀 직접등록은 "가로 사이즈 최대 966px, 높이는 50~600까지 조절"로 제한되기 때문이다(Everyday Vignettes 가이드 확인).

**C. 투명 위젯 만들기**
1. 투명 PNG(또는 배경색과 같은 색 이미지)를 만들어 **내 블로그에 글로 업로드**한다(스마트에디터 2.0에서 이미지의 HTML/이미지 주소 확보).
2. 업로드한 이미지에서 **이미지 주소(절대 URL)를 복사**한다.
3. **레이아웃·위젯 설정 > (우측 하단) + 위젯직접등록** 클릭 → 위젯명 입력 → 위젯코드(아래) 입력 → 다음.
4. 등록한 위젯을 체크하고 드래그해 원하는 위치(메뉴 그림 위)로 배치 후 적용. 한 줄에는 최대 5개까지 들어간다(Velog 'susu1991 — 네이버 홈페이지형 블로그 꾸미기'에서 "위젯을 한 줄에 최대 5개 넣을 수 있는데"로 확인).

**D. 위젯에 링크 거는 방법**
- 새 창(외부 링크):
```html
<a href="https://링크주소" target="_blank"><img src="이미지주소" width="170"></a>
```
- 현재 창(내부 카테고리 이동, 이미지맵 사용 — 네이버 블로그는 현재창 오픈에 이미지맵이 필요):
```html
<img src="이미지주소" width="170" height="50" usemap="#mm1" border="0"/>
<map name="mm1">
  <area shape="rect" coords="0,0,170,50" href="카테고리주소" target="_top"/>
</map>
```
- 카테고리 주소는 내 블로그에서 해당 카테고리를 클릭했을 때 주소창 URL을 그대로 복사해 사용. 위젯마다 map name(mm1, mm2 …)을 다르게 지정한다.

**E. 타이틀 영역 투명 처리**
- 세부 디자인 설정의 타이틀 항목에서 **'블로그 제목 표시'를 끄면** 제목 텍스트가 사라진다. 레이아웃에서 타이틀 체크를 해제하면 영역 자체가 빠진다. 이로써 스킨배경 이미지가 상단부터 끊김 없이 메뉴까지 보이게 된다.

### 4. 권장 이미지 사이즈(정리)
- 스킨배경: 가로 최대 3000px, 핵심 콘텐츠는 가운데 850~966px 안전 영역.
- 와이드 타이틀(스킨배경 활용): 가로 1920px / 세로 50~600px.
- 기본 타이틀: 가로 966px / 세로 50~600px.
- 위젯 1칸: 가로 170px 고정, 세로 최대 600px, 한 줄 최대 5개.
- 위젯 간격: 약 10px(널리 통용되나 공식 수치로 직접 확인되지 않음 — 디자인 시 본문 폭에 맞춰 미세 조정 권장).

### 5. 모바일 대응
- PC 홈형 스킨(스킨배경+위젯)은 **모바일 앱/웹에 전혀 적용되지 않는다**. 모바일은 블로그 앱 **[홈 편집]**에서 커버 이미지·커버 스타일을 고르고, 커버 구성 블록을 별도로 설정한다.
- 전자신문(2016.8.2 '네이버, 모바일블로그 개인설정 강화')에 따르면 "'홈 편집' 메뉴에서 커버스타일을 8가지 중 하나로 선택할 수 있다"고 보도됐으며, "커버를 구성하는 5개 블록(외부채널글, 외부채널목록, 인기글, 소개글, 수상내역) 노출 여부와 순서를 직접 설정한다"고 명시했다(현재 옵션 수는 이후 개편으로 달라졌을 수 있음).
- 따라서 대문 이미지와 별개로 **모바일 커버 전용 이미지**를 따로 만들고, 핵심 메뉴는 모바일에서 외부채널/대표글로 노출되게 설계해야 한다.

### 6. 클릭 영역(위젯) 좌표 맞추기 실전 팁
- 디자인 단계에서 **메뉴 그림의 좌표·크기를 위젯 단위(170px 폭)에 맞춰 설계**한다. 한 줄 5개 = 약 850px이므로 메뉴를 5등분 그리드로 배치하면 위젯과 정확히 맞는다.
- 위치 검증 시 **투명 대신 검은색(불투명) 이미지로 먼저 등록**해 클릭 영역이 메뉴 그림과 겹치는지 확인한 뒤 투명 이미지로 교체한다.
- 위젯이 너무 아래로 내려가면 빈(여백) 위젯이나 `<br>`로 높이를 조절해 위치를 맞춘다.

### 7. 흔한 실수와 트러블슈팅
- **위젯이 안 보임**: 레이아웃에서 해당 위젯 체크가 해제됐거나, 위젯이 본문 영역 밖으로 밀린 경우. 레이아웃·위젯 설정에서 체크·위치 재확인.
- **"직접등록한 위젯의 이미지 정보가 올바르지 않습니다"**: 이미지 주소(링크)가 잘못된 경우가 대부분. 내 블로그에 올린 이미지의 절대 URL을 다시 정확히 복사.
- **링크가 안 걸림**: `<a>`/`<map>` 태그 오타, coords 좌표 어긋남, target 속성 누락. 내부 이동은 `target="_top"` + 이미지맵 필요.
- **사이즈 틀어짐**: 스킨배경 높이를 이미지로 강제로 늘리면 가로가 함께 줄어 찌그러짐 → 원본 PSD/피그마에서 재추출. 타이틀 높이는 세부 디자인에서 미리 조절 후 등록.
- **이미지가 안 뜸**: http(비보안) 이미지 주소 사용 시 혼합 콘텐츠 차단 가능 → https 주소 사용.

### 8. 개발자에게 유용한 팁
- 위젯 직접등록은 `<a>`, `<img>`, `<map>`/`<area>` 등 제한된 HTML만 허용된다. 외부 JS는 막혀 있으므로 인터랙션은 이미지맵·링크 수준에서 설계.
- 좌표 기반 이미지맵(`coords="x1,y1,x2,y2"`)으로 한 위젯 안에 다중 링크를 만들 수 있어, 위젯 개수 한도를 절약할 수 있다.
- **반응형 한계**: 위젯 폭이 170px로 고정되고 모바일 미적용이라 진정한 반응형은 불가능하다. PC용 정밀 디자인 + 모바일 커버 별도 운영의 '투트랙'이 현실적이다.

## Recommendations
1. **1단계(설계)**: 피그마에서 3000px 캔버스 + 가운데 966px 안전영역을 잡고 카페/맛집/여행/지원금 4~5개 메뉴를 170px 그리드로 배치, 각 좌표 기록.
2. **2단계(업로드)**: 투명 위젯용 PNG와 메뉴 이미지를 블로그에 올려 절대 URL 확보 → 스킨배경 직접등록으로 대문 적용 → 타이틀 표시 끄기.
3. **3단계(위젯)**: 위젯직접등록으로 메뉴별 위젯 생성·링크 연결, 검은색으로 위치 검증 후 투명 교체.
4. **4단계(모바일)**: 앱 [홈 편집]에서 커버·외부채널 별도 구성.
5. **SEO 연속성(수원→가산)**: 스킨은 SEO에 직접 영향이 적다. 프로필·소개·연락처의 지역 정보를 가산으로 갱신하되, 기존 수원 키워드 글은 삭제하지 말고 유지하고, 신규 글에 가산 키워드를 점진 추가하는 방식 권장.
- **기준 변경 트리거**: 네이버가 관리 UI 명칭을 바꾸거나(과거 '디자인 설정'↔'꾸미기 설정' 변동 사례 있음) 모바일 커버 기능을 개편하면 경로 재확인 필요.

## Caveats
- 본 가이드의 경로·수치는 네이버 공식 고객센터(help.naver.com) 페이지를 직접 인용하지 못했고, 다수의 제3자 제작 가이드(Canva 공식 블로그, Everyday Vignettes, Velog, 디비카트 등 2019~2025년 자료)에서 교차 확인한 것이다. 출처 간 일관성은 높으나 2026년 최신 UI는 직접 화면에서 재확인 권장.
- '위젯 간격 10px', '위젯 전체(블로그당) 최대 개수'는 신뢰할 출처에서 확정 인용을 확보하지 못했다(한 줄 최대 5개만 확정).
- 모바일 커버 '8가지 스타일'·'5개 블록'은 전자신문 2016년 개편 보도 기준으로, 현재 옵션 수·명칭은 이후 개편으로 달라졌을 수 있다.
